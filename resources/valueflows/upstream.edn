;; The pinned upstream this repository mirrors.
;;
;; resources/valueflows/vocabulary.edn is GENERATED from all_vf.ttl by
;; bin/ingest.cljs. Neither file is hand-edited. To take a new upstream
;; release: replace all_vf.ttl, update :sha256 / :fetched-at / :bytes here,
;; re-run `nbb bin/ingest.cljs`, and read the diff.
;;
;; :sha256 is what makes "which Valueflows" answerable. Without it the EDN
;; would claim to be the spec while being whatever someone last edited.

{:schema "valueflows.upstream.v1"

 :ontology
 {:base           "https://w3id.org/valueflows/ont/vf"
  :source-url     "https://codeberg.org/valueflows/pages/raw/branch/main/assets/all_vf.TTL"
  :documentation  "https://www.valueflo.ws/"
  :algorithms-doc "https://www.valueflo.ws/algorithms/overview/"
  :file           "resources/valueflows/all_vf.ttl"
  :sha256         "47cada8122561100520702e2998c2949f6f4f42ba550c36797791882cbc48ece"
  :bytes          87877
  :fetched-at     "2026-08-15"
  :license        "Creative Commons Attribution 4.0 (upstream declares cc:license)"}

 ;; Why the raw TTL and not the rendered HTML: the action behaviour table —
 ;; the operative part of the vocabulary, the one that says what `produce`
 ;; does to an inventory — is published on
 ;; https://www.valueflo.ws/concepts/actions/ as a PNG. It is machine-readable
 ;; only in the TTL. A mirror built from the HTML would have had to
 ;; transcribe an image by eye, i.e. invent it.
 :why-ttl "the action behaviour table is an image in the HTML and RDF in the TTL"

 ;; Recorded because a name is not evidence of ownership (the same
 ;; distinction manifest/origin-domains.edn draws).
 :origin-domain
 {:domain          "valueflo.ws"
  :derived-prefix  "ws-valueflo"
  :dns-checked-at  "2026-08-15"
  :dns-evidence    "dig +short NS valueflo.ws -> ns-5-a.gandi.net, ns-50-b.gandi.net, ns-84-c.gandi.net"
  :a-record        "valueflo.ws -> valueflows.codeberg.page (Codeberg Pages)"
  :authority-binding :asserted-not-measured}}
